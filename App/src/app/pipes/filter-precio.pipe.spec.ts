import { FilterPrecioPipe } from './filter-precio.pipe';

describe('FilterPrecioPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterPrecioPipe();
    expect(pipe).toBeTruthy();
  });
});
