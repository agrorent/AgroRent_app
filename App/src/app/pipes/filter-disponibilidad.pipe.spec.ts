import { FilterDisponibilidadPipe } from './filter-disponibilidad.pipe';

describe('FilterDisponibilidadPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterDisponibilidadPipe();
    expect(pipe).toBeTruthy();
  });
});
